[buildout]

extends =
    dev.cfg

auto-checkout +=
    plonemeeting.communes

parts +=
    testmc

[instance1]
eggs +=
    plonemeeting.communes

zcml +=
    plonemeeting.communes

[sources]

[testmc]
recipe = zc.recipe.testrunner
defaults = ['--auto-color', '--auto-progress', '-s', 'plonemeeting.communes']
eggs =
    ${test:eggs}
    plonemeeting.communes [test]
environment = testenv
